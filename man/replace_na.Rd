% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_na.R
\name{replace_na}
\alias{replace_na}
\title{Replace Missing Values Using Various Methods}
\usage{
replace_na(x, method = "linear", value = NULL, min_gap = 1, max_gap = Inf, ...)
}
\arguments{
\item{x}{A vector containing numeric data with missing values (NAs)}

\item{method}{Character string specifying the replacement method:
\itemize{
\item "linear": Linear interpolation (default)
\item "spline": Spline interpolation for smoother curves
\item "stine": Stineman interpolation preserving data shape
\item "locf": Last observation carried forward
\item "value": Replace with a constant value
}}

\item{value}{Numeric value for replacement when method = "value"}

\item{min_gap}{Integer specifying minimum gap size to interpolate/fill. Gaps shorter
than this will be left as NA. Default is 1 (handle all gaps).}

\item{max_gap}{Integer or Inf specifying maximum gap size to interpolate/fill. Gaps longer
than this will be left as NA. Default is Inf (no upper limit).}

\item{...}{Additional parameters passed to the underlying interpolation functions}
}
\value{
A numeric vector with NA values replaced according to the specified method
where gap length criteria are met.
}
\description{
A wrapper function that replaces missing values using various interpolation
or filling methods.
}
\examples{
\dontrun{
x <- c(1, NA, NA, 4, 5, NA, NA, NA, 9)

# Different methods
replace_na(x, method = "linear")
replace_na(x, method = "spline")
replace_na(x, method = "stine")
replace_na(x, method = "locf")
replace_na(x, method = "value", value = 0)

# With gap constraints
replace_na(x, method = "linear", min_gap = 2)
replace_na(x, method = "spline", max_gap = 2)
replace_na(x, method = "linear", min_gap = 2, max_gap = 3)
}

}
\seealso{
\itemize{
\item replace_na_linear() for linear interpolation details
\item replace_na_spline() for spline interpolation details
\item replace_na_stine() for Stineman interpolation details
\item replace_na_locf() for last observation carried forward details
\item replace_na_value() for constant value replacement details
}
}
