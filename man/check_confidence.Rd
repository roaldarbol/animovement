% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_confidence.R
\name{check_confidence}
\alias{check_confidence}
\title{Visualize the distribution of confidence values for each keypoint}
\usage{
check_confidence(data)
}
\arguments{
\item{data}{A data frame containing at least the columns \code{keypoint} and \code{confidence}.}
}
\value{
A \code{patchwork} object combining histograms for each keypoint, visualizing
the confidence value distributions.
}
\description{
This function generates histograms showing the distribution of confidence
values for each keypoint in the dataset.
}
\details{
\itemize{
\item Each keypoint in the dataset is assigned its own histogram, showing the
frequency of different confidence values.
\item Confidence values are grouped and visualized using the \code{subplot_confidence}
function.
\item The combined plots use \code{patchwork} for alignment and styling.
}
}
\examples{
library(dplyr)
library(patchwork)
data <- tibble::tibble(
  keypoint = rep(c("head", "arm", "leg", "torso"), each = 10),
  confidence = runif(40, min = 0, max = 1)
)
# Generate histograms of confidence distributions
check_confidence(data)

}
