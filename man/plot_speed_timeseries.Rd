% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_speed_timeseries.R
\name{plot_speed_timeseries}
\alias{plot_speed_timeseries}
\title{Plot Time Series of Keypoint Speed}
\usage{
plot_speed_timeseries(data, y_max = NULL)
}
\arguments{
\item{data}{A data frame containing tracked keypoint data with the following columns:
\itemize{
\item \code{time}: Numeric time values
\item \code{keypoint}: Factor specifying the keypoint names
\item \code{x}: x-coordinates
\item \code{y}: y-coordinates
}}

\item{y_max}{Optional numeric value specifying the maximum value for the y-axis.
If NULL (default), the y-axis limit is automatically determined from the data.}
}
\value{
A ggplot object combining individual time series plots for each keypoint
using patchwork. The plots are stacked vertically with shared axes and legends.
}
\description{
Creates a multi-panel visualization of keypoint speed data over time.
Each keypoint gets its own panel showing its speed,
useful for analyzing movement intensity and identifying potential tracking issues.
}
\examples{
\dontrun{
# Plot with automatic y-axis scaling
plot_speed_timeseries(movement_data)

# Plot with fixed maximum speed of 100
plot_speed_timeseries(movement_data, y_max = 100)
}

}
\seealso{
\itemize{
\item \code{plot_position_timeseries()} for plotting position data
\item \code{calculate_speed()} for the speed calculation
}
}
