% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_na_gapsize.R
\name{check_na_gapsize}
\alias{check_na_gapsize}
\title{Visualize the occurrence of gap sizes in the data}
\usage{
check_na_gapsize(data, limit = 10, include_total = TRUE, by_keypoint = TRUE)
}
\arguments{
\item{data}{A data frame containing at least the columns \code{x}
and \code{keypoint}.}

\item{limit}{An integer specifying the maximum gap size to include in the plot.
Default is 10.}

\item{include_total}{Logical. If \code{TRUE}, includes the total count of gaps of
each size in the plot. Default is \code{TRUE}.}

\item{by_keypoint}{Logical. If \code{TRUE}, generates a separate plot for each keypoint.
If \code{FALSE}, creates a single aggregated plot for all keypoints. Default is \code{TRUE}.}
}
\value{
A \code{patchwork} object combining one or more ggplots that visualize the
occurrence of gap sizes (consecutive \code{NA}s) in the data.
}
\description{
This function generates a plot showing the distribution of gap sizes (consecutive
\code{NA} values) in the data, either aggregated or broken down by keypoints.
}
\details{
\itemize{
\item The plot highlights the most common gap sizes in the data, ordered by frequency.
\item Different colors represent the occurrence (\code{indianred}), total counts (\code{steelblue}),
and border outlines (\code{black}).
\item The function uses \code{patchwork} to combine multiple plots when \code{by_keypoint = TRUE}.
}
}
\examples{
library(dplyr)
library(ggplot2)
library(patchwork)
data <- dplyr::tibble(
  x = c(NA, NA, 3, NA, 5, 6, NA, NA, NA, 10),
  keypoint = factor(rep(c("head", "arm"), each = 5))
)
check_na_gapsize(data, limit = 5, include_total = TRUE, by_keypoint = TRUE)

}
